check-pr-conventional-name:
  name: "Check if PR name follows conventional commit standards"
  runs-on: ubuntu-latest
  steps:
    - name: "Check if PR name follows conventional commit standards"
      uses: ansys/actions/check-pr-conventional-name@{{ version }}
