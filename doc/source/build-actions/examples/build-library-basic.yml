build-library:
  name: "Build library basic example"
  runs-on: ubuntu-latest
  steps:
    - name: "Build library source and wheel artifacts"
      uses: ansys/actions/build-library@{{ branch }}
      with:
        library-name: "ansys-<product>-<library>"
